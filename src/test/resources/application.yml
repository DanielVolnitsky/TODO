server:
  port: 8081

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    #BEGIN will be dynamically set based on 'testcontainers's container attributes
    url:
    username:
    password:
    #END

    driver-class-name: org.postgresql.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      connection-test-query: SELECT 1
